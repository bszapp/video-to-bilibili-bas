node 2-批量转换svg.js -d 要处理的目录路径 -c 并发处理数量（默认4）

node 3-批量转换json.js -d 要处理的目录路径 -s 开始生成的json文件（默认1）
【特别说明：本程序存在内存泄露，请同时打开任务管理器，如果总内存占用超过85%请按下ctrl+c结束，并根据输出的最后一个文件名 使用-s参数输入下一个json文件的编号】

node 4-json数据转换bas弹幕.js -w svgViewbox宽度*10 -h svgViewbox高度*10 -maxsize 生成的单个文件最大字数（避免字数过长无法发送，默认500000） -fps 第一步处理时填写的fps（默认5） -starttime 文件名时间偏移（方便发送弹幕填写时间，默认3000）
